name: first workflow

on:
  push:
    branches: 
     - main

jobs:
  example-job:
    runs-on: ubuntu-latest


    steps:
      - name: Print a welcome message
        run: echo "Wlecome ot action workflow"

      - name: checkout repository
        uses: actions/checkout@v6.0.2


      - name: list repositiruy files
        run: ls -la
